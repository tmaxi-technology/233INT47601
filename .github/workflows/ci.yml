name: Test CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: hien thi thong bao 1
      run: echo "Hello"
    
    - name: hien thi thong bao 2s
      run: git checkout https://github.com/tmaxi-technology/AppliedBioinformatics2023.git

